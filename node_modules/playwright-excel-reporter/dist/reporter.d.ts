import type { FullConfig, FullResult, Reporter, Suite, TestCase } from '@playwright/test/reporter';
import { IExcelConfig, ExcelData, IExcelObj } from './types';
export declare class ExcelReporter implements Reporter {
    suites: Suite[];
    INVALID_TYPES: string[];
    config: IExcelConfig;
    onBegin(config: FullConfig, suite: Suite): void;
    onEnd(result: FullResult): void;
    translateExcelData(data: ExcelData): ExcelData;
    parseSuiteList(suites: Suite[]): ExcelData;
    parseTests(caseList: TestCase[], title: string, fileName: string): IExcelObj[];
    mergeSameTests(data: ExcelData): ExcelData;
}
